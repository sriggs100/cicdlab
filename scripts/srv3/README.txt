
Scripts to run on srv3 server.

These scripts are to be installed under ~activemq/bin and executed logged as activemq user


This server is the Acquirer Simulator server.

These scripts are responsible managing the ActiveMQ service.

Add the following to .profile:

# Number of open files
ulimit -n 10000

